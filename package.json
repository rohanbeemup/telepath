{
  "name": "telepath",
  "version": "0.2.0",
  "description": "Run and steer multiple Claude Code sessions from Telegram — one forum topic per resident session, with per-command approval. Powered by Claude.",
  "type": "module",
  "license": "MIT",
  "private": false,
  "scripts": {
    "start": "bun run daemon.ts",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json",
    "check": "bun run typecheck && bun test",
    "smoke": "bun scripts/smoke-session.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.278",
    "grammy": "^1.30.0",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  }
}
